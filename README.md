# vscode-mcp-lsp-gateway

VS Code extension that runs a local-only MCP server exposing read-only LSP navigation tools.

## Publishing

`package.json` uses a placeholder `publisher`. Set a real publisher in your release workflow before packaging or publishing.
