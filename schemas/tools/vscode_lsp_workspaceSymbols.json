{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/tools/vscode_lsp_workspaceSymbols.json",
  "title": "vscode_lsp_workspaceSymbols — input schema",
  "description": "Search workspace symbols by query string (paged).",
  "type": "object",
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "maxLength": 512,
      "description": "Free-text symbol query."
    },
    "cursor": {
      "type": ["string", "null"],
      "default": null,
      "description": "Opaque cursor for deterministic paging; null for first page."
    },
    "pageSize": {
      "type": "integer",
      "minimum": 1,
      "default": 100,
      "description": "Maximum number of items to return for this page."
    }
  }
}
