{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "schemas/tools/vscode.lsp.diagnostics.document.json",
  "title": "vscode.lsp.diagnostics.document — input schema",
  "description": "Return diagnostics for a single document.",
  "type": "object",
  "additionalProperties": false,
  "required": ["uri"],
  "properties": {
    "uri": {
      "type": "string",
      "minLength": 1,
      "pattern": "^file:",
      "description": "Canonical file: URI for the document."
    }
  }
}
